1.ForEach():-
        ForEach method is similar to the for loop but in for each we cannot give the starting and 
        breaking point.On the other hand in for loop we can give the breaking and starting point.

2.Filter():-
        Filter is used to get all elements that pass the condition these all elements are add in new 
        array.Filter always return a value.

3.Map():-
        Map is used to make changes in the all array elements.
        